// Code generated by CRD generation pipeline. DO NOT EDIT.

package v1alpha1

import (
	"encoding/json"
	"testing"
)

func TestEventGatewayBackendClusterAuthenticationUnmarshalJSON_NilReceiver(t *testing.T) {
	t.Parallel()

	tests := []struct {
		name    string
		payload []byte
	}{
		{name: "anonymous", payload: []byte("{\"type\":\"anonymous\",\"anonymous\":{}}")},
		{name: "sasl_plain", payload: []byte("{\"type\":\"sasl_plain\",\"sasl_plain\":{}}")},
		{name: "sasl_scram", payload: []byte("{\"type\":\"sasl_scram\",\"sasl_scram\":{}}")},
	}

	for _, tt := range tests {
		tt := tt
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()

			var target *EventGatewayBackendClusterAuthentication
			err := target.UnmarshalJSON(tt.payload)
			if err == nil {
				t.Fatal("expected error for nil receiver")
			}
			if got, want := err.Error(), "unmarshaling EventGatewayBackendClusterAuthentication: nil receiver"; got != want {
				t.Fatalf("unexpected error: got %q want %q", got, want)
			}
		})
	}
}

func TestEventGatewayBackendClusterAPISpecUnmarshalJSON_DecodesUnionFields(t *testing.T) {
	t.Parallel()

	tests := []struct {
		name   string
		payload []byte
		assert func(*testing.T, EventGatewayBackendClusterAPISpec)
	}{
		{
			name: "Authentication/anonymous",
			payload: []byte("{\"authentication\":{\"type\":\"anonymous\",\"anonymous\":{}}}"),
			assert: func(t *testing.T, target EventGatewayBackendClusterAPISpec) {
				t.Helper()
				if target.Authentication == nil {
					t.Fatalf("Authentication should be allocated")
				}
				if got, want := target.Authentication.Type, EventGatewayBackendClusterAuthenticationTypeAnonymous; got != want {
					t.Fatalf("unexpected type: got %q want %q", got, want)
				}
				if target.Authentication.Anonymous == nil {
					t.Fatalf("Authentication.Anonymous should be allocated")
				}
			},
		},
		{
			name: "Authentication/sasl_plain",
			payload: []byte("{\"authentication\":{\"type\":\"sasl_plain\",\"sasl_plain\":{}}}"),
			assert: func(t *testing.T, target EventGatewayBackendClusterAPISpec) {
				t.Helper()
				if target.Authentication == nil {
					t.Fatalf("Authentication should be allocated")
				}
				if got, want := target.Authentication.Type, EventGatewayBackendClusterAuthenticationTypeSaslPlain; got != want {
					t.Fatalf("unexpected type: got %q want %q", got, want)
				}
				if target.Authentication.SaslPlain == nil {
					t.Fatalf("Authentication.SaslPlain should be allocated")
				}
			},
		},
		{
			name: "Authentication/sasl_scram",
			payload: []byte("{\"authentication\":{\"type\":\"sasl_scram\",\"sasl_scram\":{}}}"),
			assert: func(t *testing.T, target EventGatewayBackendClusterAPISpec) {
				t.Helper()
				if target.Authentication == nil {
					t.Fatalf("Authentication should be allocated")
				}
				if got, want := target.Authentication.Type, EventGatewayBackendClusterAuthenticationTypeSaslScram; got != want {
					t.Fatalf("unexpected type: got %q want %q", got, want)
				}
				if target.Authentication.SaslScram == nil {
					t.Fatalf("Authentication.SaslScram should be allocated")
				}
			},
		},
	}

	for _, tt := range tests {
		tt := tt
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()

			var target EventGatewayBackendClusterAPISpec
			if err := json.Unmarshal(tt.payload, &target); err != nil {
				t.Fatalf("json.Unmarshal() error = %v", err)
			}
			tt.assert(t, target)
		})
	}
}
