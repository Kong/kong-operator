# HTTPRoute Path Matching test scenario
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: httproute-path-matching
spec:
  description: |
    This test validates HTTPRoute path matching scenarios including PathPrefix,
    PathExact, multiple path matches in single rule, and the strip-path annotation.

  bindings:
    - name: kong_namespace
      value: (join('-', ['kong', 'httproute-path-matching', (env('GITHUB_RUN_ID') || env('TEST_ID') || 'local')]))
    - name: konnect_api_auth_name
      value: konnect-api-auth-dev-1
    - name: gateway_class_name
      value: (join('-', ['kong', 'httproute-path-matching', (env('GITHUB_RUN_ID') || env('TEST_ID') || 'local')]))
    - name: gateway_configuration_name
      value: ($gateway_class_name)
    - name: gateway_name
      value: ($gateway_class_name)
    - name: httproute_name
      value: path-matching

  timeouts:
    apply: 120s
    assert: 300s
    cleanup: 120s
    delete: 120s

  steps:
    # Step 1: Create prerequisite resources
    - name: create-prerequisites
      description: Create GatewayClass, Gateway, and backend Service
      try:
        - apply:
            file: 01-prerequisites.yaml
        - apply:
            file: 00-httpbin.yaml
        # Assert common Konnect and Gateway resources
        - assert:
            file: ../../common/assertions/konnect-apiauth.yaml
        - assert:
            file: 01-assert-prerequisites.yaml
        - assert:
            file: 00-assert-httpbin.yaml
      catch:
        - description: Get Gateway resource status
          get:
            apiVersion: gateway.networking.k8s.io/v1
            kind: Gateway
            namespace: ($kong_namespace)
        - description: Get Pods in kong namespace
          get:
            apiVersion: v1
            kind: Pod
            namespace: ($kong_namespace)
        - description: Get operator logs
          podLogs:
            namespace: kong-system
            selector: control-plane=controller-manager
            tail: 100

    # Step 2: Test PathPrefix matching
    - name: test-pathprefix-matching
      description: Create HTTPRoute with PathPrefix matching
      try:
        - apply:
            file: 02-httproute-pathprefix.yaml
        - assert:
            file: 02-assert-httproute-pathprefix.yaml
        # Get the Gateway proxy IP
        - script:
            env:
              - name: GATEWAY_NAME
                value: ($gateway_name)
              - name: GATEWAY_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get gateway ${GATEWAY_NAME} -n ${GATEWAY_NAMESPACE} -o jsonpath='{.status.addresses[0].value}'
            outputs:
              - name: proxy_ip
                value: ($stdout)
        # Test PathPrefix matching - /prefix/anything should match
        - description: Test PathPrefix matching from within cluster
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              POD_NAME="curl-test-$(date +%s)-$$"
              kubectl run ${POD_NAME} --image=curlimages/curl:latest --rm -i --restart=Never -n ${KONG_NAMESPACE} -- \
                curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w '%{http_code}' http://${PROXY_IP}/prefix/status/200
            check:
              (contains($stdout, '200')): true
        - description: Test PathPrefix matching from host
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/prefix/status/200
            check:
              (contains($stdout, '200')): true
      catch:
        - description: Get HTTPRoute status
          get:
            apiVersion: gateway.networking.k8s.io/v1
            kind: HTTPRoute
            namespace: ($kong_namespace)
        - description: Get KongRoute resources
          get:
            apiVersion: configuration.konghq.com/v1alpha1
            kind: KongRoute
            namespace: ($kong_namespace)

    # Step 3: Test PathExact matching
    - name: test-pathexact-matching
      description: Update HTTPRoute to use PathExact matching
      try:
        - apply:
            file: 03-httproute-pathexact.yaml
        - assert:
            file: 03-assert-httproute-pathexact.yaml
        # Wait for KongRoute to be reprogrammed
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl wait --for=condition=Programmed --timeout=60s kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute
        # Get the Gateway proxy IP
        - script:
            env:
              - name: GATEWAY_NAME
                value: ($gateway_name)
              - name: GATEWAY_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get gateway ${GATEWAY_NAME} -n ${GATEWAY_NAMESPACE} -o jsonpath='{.status.addresses[0].value}'
            outputs:
              - name: proxy_ip
                value: ($stdout)
        # Test PathExact matching - /get should match exactly
        - description: Test PathExact matching - exact path should return 200
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/get
            check:
              (contains($stdout, '200')): true
        # Test that subpath does NOT match with Exact
        - description: Test PathExact matching - subpath should return 404
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              HTTP_CODE=$(curl --retry 5 --retry-delay 3 -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/get/subpath)
              echo $HTTP_CODE
            check:
              (contains($stdout, '404')): true
      catch:
        - description: Get HTTPRoute status
          get:
            apiVersion: gateway.networking.k8s.io/v1
            kind: HTTPRoute
            namespace: ($kong_namespace)
        - description: Get KongRoute resources
          get:
            apiVersion: configuration.konghq.com/v1alpha1
            kind: KongRoute
            namespace: ($kong_namespace)

    # Step 4: Test multiple path matches in single rule
    - name: test-multiple-paths-single-rule
      description: Update HTTPRoute to use multiple path matches (Exact and PathPrefix) in a single rule
      try:
        # Collect old KongRoute name before update
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute -o jsonpath='{.items[*].metadata.name}'
            outputs:
              - name: old_kongroute_names
                value: ($stdout)
        # Apply the update
        - apply:
            file: 04-httproute-multipaths.yaml
        - assert:
            file: 04-assert-httproute-multipaths.yaml
        # Wait for old KongRoute to be deleted
        - script:
            env:
              - name: OLD_NAMES
                value: ($old_kongroute_names)
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              if [ -n "$OLD_NAMES" ]; then
                for name in $OLD_NAMES; do
                  echo "Waiting for old KongRoute $name to be deleted..."
                  kubectl wait --for=delete kongroute/$name -n ${KONG_NAMESPACE} --timeout=60s || true
                done
              fi
        # Wait for new KongRoute to be programmed
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl wait --for=condition=Programmed --timeout=120s kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute
        # Verify KongRoute has multiple paths
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              PATHS=$(kubectl get kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute -o jsonpath='{.items[0].spec.paths}')
              echo "Paths: $PATHS"
              echo "$PATHS" | grep -q "/get" && echo "$PATHS" | grep -q "/post" && echo "$PATHS" | grep -q "/status"
        # Get the Gateway proxy IP
        - script:
            env:
              - name: GATEWAY_NAME
                value: ($gateway_name)
              - name: GATEWAY_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get gateway ${GATEWAY_NAME} -n ${GATEWAY_NAMESPACE} -o jsonpath='{.status.addresses[0].value}'
            outputs:
              - name: proxy_ip
                value: ($stdout)
        # Test Exact path /get - should return 200
        - description: Test Exact path /get returns 200
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" -X GET http://${PROXY_IP}/get
            check:
              (contains($stdout, '200')): true
        # Test Exact path /post with POST method - should return 200
        - description: Test Exact path /post with POST method returns 200
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" -X POST http://${PROXY_IP}/post
            check:
              (contains($stdout, '200')): true
        # Test PathPrefix /status/200 - should return 200
        - description: Test PathPrefix /status/200 returns 200
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/status/200
            check:
              (contains($stdout, '200')): true
        # Test PathPrefix /status/201 - should return 201
        - description: Test PathPrefix /status/201 returns 201
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/status/201
            check:
              (contains($stdout, '201')): true
        # Test unmatched path /other - should return 404
        - description: Test unmatched path /other returns 404
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              HTTP_CODE=$(curl --retry 5 --retry-delay 3 -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/other)
              echo $HTTP_CODE
              if [ "$HTTP_CODE" = "404" ]; then
                echo "OK"
              else
                echo "FAIL"
                exit 1
              fi
      catch:
        - description: Get HTTPRoute status
          get:
            apiVersion: gateway.networking.k8s.io/v1
            kind: HTTPRoute
            namespace: ($kong_namespace)
        - description: Get KongRoute resources
          get:
            apiVersion: configuration.konghq.com/v1alpha1
            kind: KongRoute
            namespace: ($kong_namespace)
        - description: Describe KongRoute
          script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute -o yaml

    # Step 5: Test strip-path annotation
    - name: test-strip-path-annotation
      description: Update HTTPRoute with strip-path annotation
      try:
        # Collect old KongRoute name before update
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute -o jsonpath='{.items[*].metadata.name}'
            outputs:
              - name: old_kongroute_names
                value: ($stdout)
        - apply:
            file: 05-httproute-strippath.yaml
        - assert:
            file: 05-assert-httproute-strippath.yaml
        # Wait for old KongRoute to be deleted
        - script:
            env:
              - name: OLD_NAMES
                value: ($old_kongroute_names)
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              if [ -n "$OLD_NAMES" ]; then
                for name in $OLD_NAMES; do
                  echo "Waiting for old KongRoute $name to be deleted..."
                  kubectl wait --for=delete kongroute/$name -n ${KONG_NAMESPACE} --timeout=60s || true
                done
              fi
        # Wait for new KongRoute to be programmed
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl wait --for=condition=Programmed --timeout=120s kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute
        # Verify KongRoute has strip_path: true
        - script:
            env:
              - name: KONG_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get kongroute -n ${KONG_NAMESPACE} -l gateway-operator.konghq.com/managed-by=HTTPRoute -o jsonpath='{.items[0].spec.strip_path}'
            check:
              ($stdout == 'true'): true
        # Get the Gateway proxy IP
        - script:
            env:
              - name: GATEWAY_NAME
                value: ($gateway_name)
              - name: GATEWAY_NAMESPACE
                value: ($kong_namespace)
            content: |
              kubectl get gateway ${GATEWAY_NAME} -n ${GATEWAY_NAMESPACE} -o jsonpath='{.status.addresses[0].value}'
            outputs:
              - name: proxy_ip
                value: ($stdout)
        # Test strip-path: /api/get should route to /get on backend (path stripped)
        - description: Test strip-path - /api/get routes to /get on backend
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/api/get
            check:
              (contains($stdout, '200')): true
        - description: Test strip-path - /api/status/200 routes to /status/200 on backend
          script:
            env:
              - name: PROXY_IP
                value: ($proxy_ip)
            content: |
              curl --fail --retry 10 --retry-delay 5 --retry-all-errors -s -o /dev/null -w "%{http_code}" http://${PROXY_IP}/api/status/200
            check:
              (contains($stdout, '200')): true
      catch:
        - description: Get HTTPRoute status
          get:
            apiVersion: gateway.networking.k8s.io/v1
            kind: HTTPRoute
            namespace: ($kong_namespace)
        - description: Get KongRoute resources
          get:
            apiVersion: configuration.konghq.com/v1alpha1
            kind: KongRoute
            namespace: ($kong_namespace)
        - description: Describe KongRoute
          describe:
            apiVersion: configuration.konghq.com/v1alpha1
            kind: KongRoute
            namespace: ($kong_namespace)
