apiVersion: v1
kind: Service
metadata:
  name: echo-with-policy
  namespace: ($namespace)
  annotations:
    konghq.com/upstream-policy: test-upstream-policy
spec:
  selector:
    app: ($echo_deployment_name)
  ports:
    - port: 80
      targetPort: 1027
      protocol: TCP
