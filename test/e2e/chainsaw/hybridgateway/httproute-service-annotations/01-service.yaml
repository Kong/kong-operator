apiVersion: v1
kind: Service
metadata:
  name: echo-with-annotations
  namespace: ($namespace)
  annotations:
    konghq.com/path: "/echo"
spec:
  selector:
    app: ($echo_deployment_name)
  ports:
    - port: 80
      targetPort: 1027
      protocol: TCP
