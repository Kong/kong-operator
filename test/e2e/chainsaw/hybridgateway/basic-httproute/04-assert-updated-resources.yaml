---
# Assert updated HTTPRoute with single PathPrefix match pointing to httpbin
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: echo
  namespace: kong
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: ($gateway_name)
  rules:
    - backendRefs:
        - name: httpbin
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /get
status:
  parents:
    - parentRef:
        group: gateway.networking.k8s.io
        kind: Gateway
        name: ($gateway_name)
      (conditions[?type == 'Accepted']):
        - status: 'True'
      (conditions[?type == 'ResolvedRefs']):
        - status: 'True'
      (conditions[?type == 'KongRouteProgrammed']):
        - status: 'True'

